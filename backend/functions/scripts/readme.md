There will be a readme file in the scripts folder that will contain the instructions on how to run the scripts.
```

<!-- TODO: -->
###
  
  ```yaml
  To populate the database with data, run the following command:
  There is an option to populate with all the available data or just a subset of the data.

  
  ```yaml
